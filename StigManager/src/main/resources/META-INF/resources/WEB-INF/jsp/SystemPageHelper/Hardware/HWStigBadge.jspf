<div class="col">
    <div class="container-fluid">
        <c:forEach items="${myStigs}" var="myStig">
            <span class="badge bg-primary p-2">${myStig.getName()} V${myStig.getVersion()} R${myStig.getRelease()}</span>
        </c:forEach>
    </div>
</div>